# Distribution ids for Premade Groups Filter - EllesmereUI, appended to the
# environment by the release workflow and passed through to the packager. The
# API tokens they authenticate with are account-wide and live in repository
# secrets, so only the ids sit here.
PACKAGER_ARGS=-p 1671480 -a BNBmogGx
